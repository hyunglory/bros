# BROS 프로젝트 작업 지침

이 파일은 BROS 저장소에서 작업하는 모든 모델과 에이전트에 적용한다. 상세 운영 기준은 `doc/BROS_개발_운영_구성_지침_v0.1.md`를 따른다.

## 단계 종료와 모델 전환

- 설계, 문서화, 구현, 검수, 운영 준비 등 하나의 작업 분야 또는 단계가 끝날 때마다 결정 기록을 남긴다.
- 모델이나 추론 수준을 바꾸기 전에는 현재 단계의 결론을 파일에 기록하고, 다음 모델이 대화만 보고 결론을 재구성하게 두지 않는다.
- 개발 저장소가 준비된 뒤의 결정 기록 Source of Truth는 `docs/DECISIONS.md`다. 해당 파일이 아직 없으면 생성한다.
- 다음 작업자는 작업을 시작하기 전에 최신 결정 기록, 관련 기준 문서, 현재 구현 상태를 읽는다.
- 확정된 결정을 임의로 재해석하거나 변경하지 않는다. 변경이 필요하면 기존 기록을 덮어쓰지 말고 새 결정으로 추가하며, 변경 근거와 대체된 결정 ID를 적는다.
- 가벼운 모델이나 낮은 추론 수준에 문서 정리를 맡길 때는 확정된 결정을 정확히 반영하는 범위로 제한한다. 새로운 설계 판단이 필요하면 임의 확정하지 않고 `결정 필요`로 남긴다.

## 결정 기록 필수 항목

각 기록에는 다음을 포함한다.

- 결정 ID와 일자
- 종료한 작업 분야 또는 단계
- 작업에 사용한 모델과 추론 수준
- 검토 범위와 근거 문서
- 확정 결정
- 기각한 선택지와 이유
- 변경 파일
- 실행한 검증과 결과
- 미해결 사항 및 blocker
- 다음 작업의 범위, 금지 변경, 완료 조건

결정 기록은 실제 증거와 상태를 구분한다. 문서 검토 완료를 구현 또는 테스트 PASS로 기록하지 않는다.

## 다음 모델의 인수 원칙

다음 모델은 직전 모델의 숨은 추론을 이어받는다고 가정하지 않는다. 파일에 남은 결정과 검증 증거만 인수한다. 최신 기록 사이에 충돌이 있으면 날짜만으로 선택하지 말고 `supersedes` 관계와 승인 근거를 확인한다. 근거 없는 충돌은 새로 확정하지 않고 사용자 결정이 필요한 항목으로 보고한다.

## 완료 보고

- 모든 작업 완료 보고에는 사용자가 지정한 `[추천모델과 추론수준]` 항목을 반드시 포함한다.
- 항목은 `다음 작업`, `추천 모델`, `추론 수준`, `추천 이유`, `모델 전환 조건` 순서로 작성한다. 모델 전환 조건은 작업 유형별 `정확한 모델명 / 추론 수준`으로 안내한다.
- 이 항목에는 다음 작업에 추천하는 정확한 모델명과 추론 수준, 추천 이유, 모델을 다시 전환해야 하는 조건을 적는다.
- 다음 작업이 없으면 `없음 — 현재 요청 완료`라고 명시한다.
- 추천은 관성적으로 최고 성능을 선택하지 않고 작업의 복잡도, 위험도, 검증 필요성, 사용량 효율을 기준으로 한다.
